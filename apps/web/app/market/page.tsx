'use client';
import { useEffect,useState } from 'react';
type Offer={slug:string;name:string;description:string;price:number};const api=process.env.NEXT_PUBLIC_API_URL??'http://localhost:3001';
export default function MarketPage(){const [offers,setOffers]=useState<Offer[]>([]);const [message,setMessage]=useState('');useEffect(()=>{fetch(`${api}/market/offers`).then(r=>r.json()).then(setOffers);},[]);const buy=async(slug:string)=>{const token=sessionStorage.getItem('accessToken');const r=await fetch(`${api}/market/buy`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({itemSlug:slug,quantity:1})});const d=await r.json();setMessage(r.ok?`Bought ${d.quantity} ${d.itemName} for ${d.cost} Cash.`:d.message);};return <main className="crafting"><p className="eyebrow">CITY EXCHANGE</p><h1>Market</h1>{message&&<p className="form-success">{message}</p>}<section className="recipe-grid">{offers.map(o=><article key={o.slug}><h2>{o.name}</h2><p>{o.description}</p><small>{o.price} Cash</small><button onClick={()=>buy(o.slug)}>Buy one</button></article>)}</section></main>}
